// src/socket/support.js
// Rôle : enregistre les événements Socket.IO du module support client
// (ticket:ouvrir, ticket:assigner, ticket:fermer, message:envoyer,
// message:lu, fichier:partager, frappe). Appelé une fois par connexion
// depuis socket/index.js. Contenu ajouté progressivement dans les
// phases suivantes dédiées aux tickets, au chat et au partage de fichiers.

module.exports = function enregistrerEvenementsSupport(io, socket) {
  // Implémenté dans les phases suivantes (tickets, chat, fichiers).
};
