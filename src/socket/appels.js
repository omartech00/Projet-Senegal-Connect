// src/socket/appels.js
// Rôle : enregistre la signalisation PeerJS (appel:initier, appel:
// accepter, appel:refuser, appel:terminer, appel:controle). Appelé
// une fois par connexion depuis socket/index.js. Contenu ajouté dans
// les phases dédiées au module M4 (WebRTC/PeerJS).

module.exports = function enregistrerEvenementsAppels(io, socket) {
  // Implémenté dans les phases suivantes (module M4).
};
